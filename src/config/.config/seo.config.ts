// 🕊️白木 原创开发 🔗gl.baimu.live
// 用户自定义 SEO 配置 - 覆盖默认配置

import type { SeoConfig } from '../seo.config';

// ========================================
// 📝 用户自定义配置
// 这里的配置会覆盖默认配置
// ========================================
export const userSeoConfig: Partial<SeoConfig> = {
  // siteName: 'My Blog',
  // defaultDescription: '自定义站点描述',
  // defaultImage: '/assets/img/custom-og.jpg',
  // titleTemplate: '%s | My Blog',
};
